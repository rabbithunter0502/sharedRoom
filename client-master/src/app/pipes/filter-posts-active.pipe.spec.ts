import { FilterPostsActivePipe } from './filter-posts-active.pipe';

describe('FilterPostsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPostsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
